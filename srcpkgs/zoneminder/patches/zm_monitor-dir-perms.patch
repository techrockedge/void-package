--- a/src/zm_monitor.cpp	2026-08-10 21:19:09.000000000 -0400
+++ b/src/zm_monitor.cpp	2026-08-22 21:10:38.800780114 -0400
@@ -766,9 +766,11 @@
   if (purpose != QUERY) {
     LoadCamera();
 
-    if ( mkdir(monitor_dir.c_str(), 0755) && ( errno != EEXIST ) ) {
-      Error("Can't mkdir %s: %s", monitor_dir.c_str(), strerror(errno));
-    }
+    if ( mkdir(monitor_dir.c_str(), 0755) && ( errno != EEXIST ) ) {
+      Error("Can't mkdir %s: %s", monitor_dir.c_str(), strerror(errno));
+    } else if ( chmod(monitor_dir.c_str(), 02740) ) {
+      Error("Can't chmod %s to 02740: %s", monitor_dir.c_str(), strerror(errno));
+    }
 
     if ( config.record_diag_images ) {
       if ( config.record_diag_images_fifo ) {
